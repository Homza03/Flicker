import 'package:flutter/material.dart';

class Score10Game extends StatelessWidget {
  const Score10Game({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Score 10')), body: const Center(child: Text('Score 10 placeholder')));
  }
}
