// Notifications service (FCM + local notifications stub)
// EN: Handles push notifications and local scheduling.
// CZ: Zpracovává push notifikace a lokální oznámení.
class NotificationsService {
  Future<void> init() async {
    // TODO: firebase messaging + local notifications init
  }

  Future<void> showDailyChallengeNotification() async {}
}
